platform: linux
image_resource:
  type: registry-image
  source:
    repository: squidfunk/mkdocs-material
    tag: 9.7.6
run:
  dir: docs
  path: /usr/local/bin/mkdocs
  args:
  - build
  - --site-dir=../docroot
inputs:
- name: docs
outputs:
- name: docroot
